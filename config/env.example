# Example environment variables for MAI_DS_2
# Copy this file to `.env` for local development and set real values.
# Do NOT commit your `.env` file.

# Admin authentication (used by admin/server.js)
ADMIN_PASSWORD=changeme

# EmailJS configuration (used by api/send-email.js)
EMAILJS_SERVICE_ID=service_example
EMAILJS_PRIVATE_KEY=prv_example_secret_key
EMAILJS_PUBLIC_KEY=your_public_key_example
EMAILJS_TEMPLATE_CONTACT=template_contact_example
EMAILJS_TEMPLATE_WORKSHOP=template_workshop_example

# Comma-separated list of allowed origins for CORS/origin checks
# Include your local dev server and production domains
ALLOWED_ORIGINS=http://localhost:3000,https://aimuseum.se,https://www.aimuseum.se

# Postgres (system-wide tools/export-db-content.js)
# Preferred: DATABASE_URL=postgres://user:pass@host:port/db
PGHOST=localhost
PGPORT=5432
PGUSER=mai_user
PGPASSWORD=changeme
PGDATABASE=mai_db
PGSSL=

# Alternative DB vars for tools/db-content.js (legacy)
DB_HOST=localhost
DB_PORT=5432
DB_USER=mai_user
DB_PASS=changeme
